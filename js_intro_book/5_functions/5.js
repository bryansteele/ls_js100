function scream(words) {
  words = words + '!!!!';
  return;
  console.log(words);
}

scream('Yipeee');

// Nothing----It calls the function and stops at the explicit return. It never had a chance to consol.log