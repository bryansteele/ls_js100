let a = 1;

function myFunction(a) {
  console.log(a);
}

let b = 2;

myFunction(b);

// 2
// Calling the function on line 9 and passing an arg into the function, which is 2. The parameter a has nothing to do with line 1 variable. Line 1 realy isn't doing anything in this program.